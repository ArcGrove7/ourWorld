# 拿到一款新的文字遊戲，怎麼從零做到自動遊玩

這份是給 AI 照做的流程。每一步寫「做什麼、怎麼驗、用工具包的哪一支」。
順序有意義：跳過前面的步驟，後面的自動化會在你看不到的地方犯錯。

整份流程的核心只有一句話：**先把遊戲當成一個有額度的 API 來量，再讓程式照量到的數字做決定。**
不要照角色印象、不要照遊戲文案、不要照「玩家都說」。

## 第 0 步：先問三件事，答不出來就不要開自動化

| 問題 | 為什麼 |
| --- | --- |
| 每小時（或每分鐘）能送幾發？站方有沒有明說？ | 這是帳號的規矩，不是行程的。打爆了輕則 429、重則封號。**`budget.js` 的 `perHour` 就是這個數字減掉緩衝** |
| 哪些操作是回不去的？（轉生、刪角、花掉不可再得的資源、打會真死的 Boss） | 這些一律預設關閉、要人明確指定才做。自動化沒有「見苗頭不對就跑」的判斷力 |
| 寫入逾時的時候，伺服器可能已經做了嗎？ | 幾乎一定可能。所以**寫入永遠不自動重送**（`http.js` 的 `post()` 預設 `retries: 0`），重送就是做了兩次 |

## 第 1 步：找 API 與前端 bundle

1. 開瀏覽器的 Network 面板玩十分鐘，存成 HAR。每一發請求的 method、路徑、request body、response body 都在裡面。
2. 找前端 bundle（通常是 `index-<hash>.js`）。裡面有：所有端點的路徑、欄位名、事件與選項的文案、日誌的顏色判斷（那些判斷會透露伺服器有哪些事件型別）。
3. **對每一條你要引用的規則，記下它來自 HAR 的第幾筆或 bundle 的哪個位元組範圍，連同檔案的 sha256。** 換了 bundle 就重驗一次。沒有出處的規則不寫進機制文件。

驗：認證方式（JWT 在 header 還是 cookie）、token 多久過期、401 長什麼樣。**token 只從環境變數讀**（`token.js` 的 `requireToken()`，它會掃命令列、看到像 token 的東西就停）。

## 第 2 步：列出行動與冷卻

1. 每一種行動各送一發，記下：回應裡帶什麼（經驗、獎勵、待處理事件）、冷卻多久、**冷卻資訊是不是就在行動的回應裡**。
2. 行動的回應通常帶了 `lastActionAt`／`cooldownMs` 之類的欄位。**用它算下一次什麼時候能動，不要每圈再打一發狀態查詢**——那一發問到的東西上一發早就給過了，額度會花掉兩倍。`cooldown.js` 做這件事，讀不到就回 `fallbackMs` 並把 `observed` 設成 false，不猜。
3. 冷卻跟哪些操作共用（PvP 跟一般行動共用冷卻是常見設計）。

驗：連送兩發，第二發應該被擋（429 或遊戲自己的錯誤碼）。記下錯誤碼與訊息，日後產線要靠它分辨「冷卻中」與「真的壞了」。

## 第 3 步：事件與選項——先記錄，再決策

大多數文字遊戲的深度在事件：行動偶爾觸發事件、事件有幾個選項、選項有判定（某一維 vs 難度）與結果（獎勵、開打、死亡、解鎖）。

1. **先用最保守的策略跑一陣子只記錄**：每一筆事件記下 `eventId`、每個選項的 `id`／名字／判定／伺服器給的成功率、你選了哪個、結果是什麼（拿了幾點、有沒有開打、有沒有死、解鎖了什麼）、**以及你當下多強**（後面會用到）。原始紀錄用 `jsonl.js` 一行一筆追加，**永遠不改舊行**。
2. 成功率**以伺服器給的為準**。自己推的公式只當後備，而且要標「估」。
3. 用 `tally.js` 把紀錄累成統計表：**存原始總計，不存平均**。「遇到 10 次共得 9 點」，不要「平均 0.9 點」——平均把分母藏起來，你會分不出量過 10 次還是 1 次。
4. 然後才把決策交給 `decide.js`。它的六條規則每一條後面都有一次真實的損失，最要緊的三條：
   - **致死率跟著你有多強走，不是選項的固定屬性**。同一個選項在高等玩得贏、轉生成低等就會死。所以記錄要帶「當時多強」。
   - **沒量過＋名字寫著要打＝不要挑**。零樣本讓每一道資料閘門都放行；第一個踩到的人永遠沒有保護。
   - **沒量過的期望值是 null，不是 0**。已知的都是 0 收益時要去試沒量過的，不然它永遠量不到。
5. 全部選項都被閘門擋住時**停下來等人**，不要硬選。

驗：把一週的紀錄重新餵給 `tally.js`，得到的表跟線上用的那份逐欄位一致。

## 第 4 步：量成長，只算乾淨樣本

升級時各維長多少、跟這一級做了什麼行動有沒有關係，是練功策略的全部依據。

1. **一次只變一個變數**：整級只做同一種行動、一次只升一級、整級在同一次執行裡。混合的那一級不進表。
2. 每個「角色 × 行動」的格子量到的九個數字如果每次都一樣，那它就是確定的規則；如果不一樣，先懷疑自己混到別的行動，再懷疑遊戲有隨機。
3. 找不變量。這個專案找到的是「每級成長的非 HP 合計 + HP ÷ 30 = 該角色權重合計」，全表無一例外。你的遊戲會有自己的。**找到不變量之後，任何一格對不上就是量錯了或站方改了數值**，這是最便宜的異常偵測。
4. 站方會不公告就改數值。**每次看盤都比對「伺服器說給了多少」與「實際變了多少」**，對不上就停下來看。

## 第 5 步：戰報逐擊解析

戰鬥系統的規則通常不在 API 的任何欄位裡，只在戰報的逐擊事件裡（誰打誰、傷害、暴擊、閃避、反擊、治療）。

1. 收戰報。很多遊戲的戰報端點不檢查戰報是不是你的，整座伺服器的對戰史都讀得到，一發 PvP 都不用打。
2. 按事件型別分組看（MISS、CRIT、COUNTER、HEAL……），每一型別各拿雙方九維做回歸。
3. **「測不出來」不等於「不存在」**：遊戲自己的能力值說明說技巧影響反擊，你的樣本雜訊太大分不出來，那只是樣本不夠，不要寫成「技巧不影響反擊」。
4. 治療這類公式常常是精確的（`回復 = 智力 × 倍率`，分毫不差），傷害則要先補回防禦折損再擬合。

## 第 6 步：安全閘門與死亡黑名單

自動化的第一要務不是賺得多，是**不要把角色玩死**。

- 黑名單**從紀錄產生，不要手寫**。手寫版會漏掉害死過人的那一條。
- 「以前沒事」不代表現在沒事：以前沒事的人比較強，不是那個選項比較安全。
- 會開打的選項預設避開；解鎖角色的選項是例外，但那是人要決定的取捨，產線標「人工」然後跳過，不替人賭。
- 死亡紀錄只增不減。重跑統計表的時候死亡數變少了，是紀錄不齊，不是那個選項變安全了——`derived.js` 的 `shrinkGate()` 擋這件事。

## 第 7 步：多帳號產線與看門狗

- 一顆帳號一條線，一條線一個行程，額度按帳號各算（`budget.js` 給同一個檔就能讓同帳號的多個行程共用帳本）。
- 看門狗每兩分鐘檢查一次每條線還活著沒，死了就重拉。看門狗本身要冪等（重跑不會多開一條）。
- **背景行程會跟著工作階段一起死**，`nohup`／`setsid` 擋不住整組被回收。在什麼環境跑就用那個環境自己追蹤的背景機制。掛完**一定要隔幾分鐘回來看 log 的時間戳**確認還在跑。
- 看門狗傳的參數順序要跟手動拉線時一模一樣，不然它會判定「參數變了」每兩分鐘重拉一次，永遠不會穩定。

## 第 8 步：原始紀錄只增不改，衍生表過閘門

- 原始紀錄是證據，一個字都不改。要修正就加一筆新的。
- 衍生表全部是產生出來的，**不要手改**——手改的數字沒有紀錄可以回溯，重跑就被蓋掉。
- 衍生表要記自己的來源（`derived.js` 的 `stamp()`），CI 重新算一次對不對得上（`verify()`）。來源改了衍生表沒重跑，是最難靠眼睛發現的一種過時：檔案還在、格式還對，只是數字是上一版的。
- 產生器要有縮水閘門（`shrinkGate()`）：總樣本、死亡數、任何只增不減的計數變少就拒寫。看到它擋就去補紀錄，不要用 `--allow-shrink` 硬推。

## 這個工具包裡每一支對應到哪一步

| 步 | 模組 | 它替你守的規矩 |
| --- | --- | --- |
| 0 | `budget.js` | 每小時額度，含重試；閘門放在送出那一層 |
| 0、1 | `http.js`、`token.js` | 寫入不重送；401 直接停；token 不落地 |
| 2 | `cooldown.js` | 從行動的回應算冷卻，不打狀態查詢；讀不到不猜 |
| 3 | `jsonl.js`、`tally.js`、`decide.js` | 紀錄只追加；統計存總計；決策先過安全閘門 |
| 8 | `derived.js` | 縮水閘門、來源印章 |
| 全部 | `cli.js` | 打錯的參數當場停，不安靜地走預設值 |
| 全部 | `tw-time.js` | 時間戳帶時區，解得回同一個瞬間 |

沒有做成模組的（第 4、5、7 步）是方法，不是程式——每款遊戲的九維、戰報格式、執行環境都不一樣，
硬做成通用模組只會變成誰都不合身的殼。這份文件把方法寫下來，程式你自己寫，規矩照上面守。
